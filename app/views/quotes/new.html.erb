<div class="page page--narrow">
  <span class="tag">Nouveau devis</span>

  <%= link_to '< Retour aux devis', quotes_path, class: 'breadcrumb' %>
  <h1 class="page-title">Nouveau devis</h1>

  <% if @quote.errors.any? %>
    <div class="error-box">
      <h2><%= pluralize(@quote.errors.count, 'erreur') %></h2>
      <ul>
        <% @quote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with(model: @quote, local: true) do |form| %>
    <div class="form-field" style="margin-bottom: 18px;">
      <%= form.label :name, 'Nom' %>
      <%= form.text_field :name, autofocus: true, placeholder: 'Devis acme - mai 2026' %>
    </div>

    <div class="form-actions">
      <%= form.submit 'Créer le devis', class: 'btn btn--primary' %>
      <%= link_to 'Annuler', quotes_path, class: 'btn' %>
    </div>
  <% end %>
</div>
